import os
from google import genai

# מיפוי השעות לניסוח טבעי בעברית
HOURS_HEBREW = {
    0: "מהדורת חצות",
    1: "מהדורת אחת בלילה",
    2: "מהדורת שתים בלילה",
    3: "מהדורת שלוש בלילה",
    4: "מהדורת ארבע לפנות בוקר",
    5: "מהדורת חמש בבוקר",
    6: "מהדורת שש בבוקר",
    7: "מהדורת שבע בבוקר",
    8: "מהדורת שמונה בבוקר",
    9: "מהדורת תשע בבוקר",
    10: "מהדורת עשר בבוקר",
    11: "מהדורת אחת עשרה בבוקר",
    12: "מהדורת שתים עשרה בצהריים",
    13: "מהדורת אחת בצהריים",
    14: "מהדורת שתים בצהריים",
    15: "מהדורת שלוש בצהריים",
    16: "מהדורת ארבע אחר הצהריים",
    17: "מהדורת חמש אחר הצהריים",
    18: "מהדורת שש בערב",
    19: "מהדורת שבע בערב",
    20: "מהדורת שמונה בערב",
    21: "מהדורת תשע בערב",
    22: "מהדורת עשר בלילה",
    23: "מהדורת אחת עשרה בלילה"
}

def edit_news_with_ai(news_text, folder, current_hour=None):
    """
    מקבלת טקסט חדשות גולמי ומחזירה מהדורה ערוכה באמצעות Google Gemini API
    """
    api_key = os.environ.get("GEMINI_API_KEY")

    if not api_key:
        raise Exception("המשתנה GEMINI_API_KEY אינו מוגדר במערכת")

    client = genai.Client(api_key=api_key)

    if current_hour is not None and current_hour in HOURS_HEBREW:
        edition_time_str = HOURS_HEBREW[current_hour]
        intro_text = f"אתם מאזינים ל{edition_time_str} בחדשות המידע."
    else:
        intro_text = "אתם מאזינים למהדורת החדשות בחדשות המידע."

    system_prompt = f"""
אתה עורך החדשות הראשי של תחנת רדיו.
מטרתך להכין מהדורת חדשות מקצועית, קצרה וקולחת המיועדת להקראה על ידי קריין.

הוראות חובה:
1. שכתב את הידיעות בלשונך תוך שמירה מלאה על העובדות.
2. אין להוסיף שום מידע שאינו מופיע בידיעות.
3. מיזוג: אם קיימות כמה ידיעות על אותו אירוע, מזג אותן לידיעה אחת מקיפה.
4. מיין את הידיעות לפי קטגוריות בסדר הבא:
   - פוליטיקה וביטחון
   - חדשות החוץ
   - חדשות כלליות
   - כלכלה ונדל"ן
   - תחבורה
   - תחזית לסיום
5. חובה לכתוב את שם הקטגוריה בצורה טבעית להקראה לפני כל קבוצת ידיעות (לדוגמה: "בפוליטיקה ובביטחון", "בחדשות החוץ", "בתחבורה", "ולסיום, תחזית מזג האוויר").
6. קטגוריית מזג האוויר תמיד תהיה אחרונה.
7. אין להזכיר מקורות, אתרי חדשות או שמות כתבים.
8. הסר מילים כגון: מבזק, עדכון, דיווח ראשוני, פרסום ראשון.
9. הפלט חייב להיות טקסט נקי בלבד - ללא כוכביות (*), מספור או כותרות Markdown.

מבנה חובה לשלוחה 1:

{intro_text}

עיקרי החדשות מהשעה האחרונה.

[כל הקטגוריות והידיעות]

עד כאן מהדורת החדשות. תודה שהאזנתם ולהתראות במהדורה הבאה.
"""

    user_prompt = f"""
מספר השלוחה: {folder}

ערוך את הידיעות למהדורת חדשות קצרה ומסודרת:

{news_text}
"""

    try:
        response = client.models.generate_content(
            model="gemini-2.5-flash",
            contents=[system_prompt, user_prompt],
        )
        return response.text.strip()
    except Exception as e:
        print(f"Error calling Gemini API: {e}")
        return news_text
